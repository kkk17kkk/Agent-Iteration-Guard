# Repository Agent Instructions

## Git 发布约定

- 用户明确要求发布时，使用本地 Git 直接执行 `git push origin main`。
- 不要求安装、检查或调用 GitHub CLI（`gh`），也不因缺少 `gh` 阻塞发布。
- 推送前仍需检查当前分支、remote、工作树范围、敏感文件和提交前校验结果。
- 只提交当前任务范围内的代码、测试和必要文档；保留无关的本地截图、临时目录及用户未授权的改动。
- 未经用户明确授权，不执行远端发布、创建 PR 或修改 remote；本文件中的直接推送约定仅适用于用户已明确要求发布的场景。
